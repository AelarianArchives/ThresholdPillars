# No LFS rules currently active.
# Audio assets are gitignored (see .gitignore). If we later decide to
# version any binary assets in Git, document the decision in CLAUDE.md
# before adding filter=lfs rules here.
